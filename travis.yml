sudo: required

language: generic

services:
  - docker

before_install:
  - echo "start Creating an image with dockerfile"
  - docker build -t kmlee95/nginx-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kmlee95/nginx-react npm run test -- --coverage

after_success:
  - echo "Test Success"
